<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    app:startDestination="@id/splashFragment"
    android:id="@+id/nav_app_graph">

    <fragment
        android:id="@+id/splashFragment"
        android:name="com.berg.rickapp.features.splash.SplashFragment"
        android:label="fragment_splash">
        <action android:id="@+id/action_splashFragment_to_tabsFragment"
            app:destination="@id/tabsFragment"
            app:popUpTo="@id/splashFragment"
            app:launchSingleTop="true"
            app:popUpToInclusive="true"/>
    </fragment>

    <fragment
        android:id="@+id/tabsFragment"
        android:name="com.berg.rickapp.features.tabs.TabsFragment"
        android:label="fragment_tabs"
        tools:layout="@layout/fragment_tabs">
        <action android:id="@+id/action_tabsFragment_to_aboutFragment"
            app:destination="@id/aboutFragment"/>
    </fragment>

    <fragment
        android:id="@+id/aboutFragment"
        android:name="com.berg.rickapp.features.tabs.temp.AboutFragment"
        android:label="fragment_about" />

</navigation>